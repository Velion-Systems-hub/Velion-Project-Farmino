1. worked on the HomePage.jsx \Bumasel\src\components\Home\HomePage.jsx
padding buttom: pb-10-tailwind css.


2. StoreInfo.jsx Bumasel\src\Pages\Vendors\Onboarding\components\StoreInfo.jsx

//if vendor is not logged in before creating store
	const isAuthenticated = useSelector(selectIsAuthenticated);
	const navigate = useNavigate();

	useEffect(() => {
     // If action requires auth and user is not authenticated, redirect to login
     if (!isAuthenticated) {
       navigate('/login');
     }
   }, [isAuthenticated, navigate]);

3. SavedProduct.jsx \Bumasel\src\Pages\Store\components\SavedProduct.jsx
padding buttom: pb-10-tailwind css.
